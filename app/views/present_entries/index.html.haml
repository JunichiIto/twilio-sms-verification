%h1 Listing present_entries

%div= link_to 'New Present entry', new_present_entry_path

%table.table.table-bordered.table-striped
  %tr
    %th Name
    %th Address
    %th Mobile phone number
    %th Email
    %th
    %th
    %th

  - @present_entries.each do |present_entry|
    %tr
      %td= present_entry.name
      %td= present_entry.address
      %td= present_entry.mobile_phone_number
      %td= present_entry.email
      %td= link_to 'Show', present_entry
      %td= link_to 'Edit', edit_present_entry_path(present_entry)
      %td= link_to 'Destroy', present_entry, :method => :delete, :data => { :confirm => 'Are you sure?' }
